# License review

BentoML `1.4.39` is Apache-2.0, and this template's original source is Apache-2.0. The template uses the official Python and uv images only as pinned build/runtime dependencies; it does not redistribute an upstream model or dataset.
